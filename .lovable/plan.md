## Что меняем

В `src/routes/index.tsx` удалить блок верхнего лейбла "EcoCub · Московская область" внутри hero-секции (строки 98–104) — `<Container>` с абсолютным позиционированием и тегом `<p>`.

## Почему

- Лейбл сливается с небом на светлых слайдах (закат, облака) и плохо читается.
- Дублирует информацию: бренд уже в логотипе шапки, регион — в футере и на странице "Контакты".
- На hero освобождается воздух для главного акцента — заголовка "КАПИТАЛЬНЫЕ. ТЕХНОЛОГИЧНЫЕ." и CTA, что соответствует Apple-style минимализму бренда.

## Затронутые файлы

- `src/routes/index.tsx` — удалить 7 строк (комментарий + Container с лейблом).

Остальная разметка hero (HeroSlider, нижняя панель с заголовком и кнопками) не меняется.
